diff --git a/packages/client/tests/functional/_utils/providers.ts b/packages/client/tests/functional/_utils/providers.ts
index 70a8297..a410ce8 100644
--- a/packages/client/tests/functional/_utils/providers.ts
+++ b/packages/client/tests/functional/_utils/providers.ts
@@ -9,6 +9,7 @@ export enum Providers {
 
 export enum AdapterProviders {
   JS_PG = 'js_pg',
+  JS_POSTGREJS = 'js_postgrejs',
   JS_PLANETSCALE = 'js_planetscale',
   JS_NEON = 'js_neon',
   JS_LIBSQL = 'js_libsql',
@@ -31,7 +32,7 @@ export enum RelationModes {
 export type GeneratorTypes = 'prisma-client-js' | 'prisma-client-ts'
 
 export const adaptersForProvider = {
-  [Providers.POSTGRESQL]: [AdapterProviders.JS_PG, AdapterProviders.JS_NEON],
+  [Providers.POSTGRESQL]: [AdapterProviders.JS_PG, AdapterProviders.JS_NEON, AdapterProviders.JS_POSTGREJS],
   [Providers.MYSQL]: [AdapterProviders.JS_PLANETSCALE, AdapterProviders.JS_MARIADB],
   [Providers.SQLITE]: [AdapterProviders.JS_LIBSQL, AdapterProviders.JS_D1, AdapterProviders.JS_BETTER_SQLITE3],
   [Providers.MONGODB]: [],
@@ -41,6 +42,7 @@ export const adaptersForProvider = {
 
 export const relationModesForAdapter = {
   [AdapterProviders.JS_PG]: undefined,
+  [AdapterProviders.JS_POSTGREJS]: undefined,
   [AdapterProviders.JS_PLANETSCALE]: RelationModes.PRISMA,
   [AdapterProviders.JS_NEON]: undefined,
   [AdapterProviders.JS_LIBSQL]: undefined,
diff --git a/packages/client/tests/functional/_utils/relationMode/conditionalError.ts b/packages/client/tests/functional/_utils/relationMode/conditionalError.ts
index 9b887a7..a6c33ed 100644
--- a/packages/client/tests/functional/_utils/relationMode/conditionalError.ts
+++ b/packages/client/tests/functional/_utils/relationMode/conditionalError.ts
@@ -51,6 +51,18 @@ class ConditionalErrorBuilder<Supplied> implements With<Supplied>, ConditionalEr
       )
     }
 
+    // js_postgrejs reads js_pg's entry. The table is keyed by adapter label
+    // and no third-party adapter has one, so without this every test using it
+    // compares against the literal TODO string below and fails for a reason
+    // that has nothing to do with the adapter.
+    if (driverAdapter === 'js_postgrejs') {
+      return (
+        errorBase['js_pg'] ||
+        errorBase['postgresql'] ||
+        `TODO: add error for provider=${provider} and driverAdapter=${driverAdapter}`
+      )
+    }
+
     return (
       errorBase[driverAdapter ?? provider] ||
       `TODO: add error for provider=${provider} and driverAdapter=${driverAdapter}`
diff --git a/packages/client/tests/functional/_utils/setupTestSuiteClient.ts b/packages/client/tests/functional/_utils/setupTestSuiteClient.ts
index 19f08e0..f190241 100644
--- a/packages/client/tests/functional/_utils/setupTestSuiteClient.ts
+++ b/packages/client/tests/functional/_utils/setupTestSuiteClient.ts
@@ -178,6 +178,14 @@ export function setupTestSuiteClientDriverAdapter({
     throw new Error(`Missing Driver Adapter`)
   }
 
+  if (driverAdapter === AdapterProviders.JS_POSTGREJS) {
+    const { PrismaPostgreJS } = require('prisma-postgrejs') as typeof import('prisma-postgrejs')
+
+    return {
+      adapter: new PrismaPostgreJS(datasourceInfo.databaseUrl),
+    }
+  }
+
   if (driverAdapter === AdapterProviders.JS_PG || driverAdapter === AdapterProviders.JS_PG_COCKROACHDB) {
     const { PrismaPg } = require('@prisma/adapter-pg') as typeof import('@prisma/adapter-pg')
 
diff --git a/packages/client/tests/functional/_utils/setupTestSuiteEnv.ts b/packages/client/tests/functional/_utils/setupTestSuiteEnv.ts
index ec319a0..49aefc2 100644
--- a/packages/client/tests/functional/_utils/setupTestSuiteEnv.ts
+++ b/packages/client/tests/functional/_utils/setupTestSuiteEnv.ts
@@ -474,6 +474,10 @@ function getDbUrlFromFlavor(driverAdapterOrFlavor: `${AdapterProviders}` | undef
       // Note: we're using Postgres 10 for Postgres (Rust driver, `pg` driver adapter),
       // and Postgres 16 for Neon due to https://github.com/prisma/team-orm/issues/511.
       .with(AdapterProviders.JS_PG, () => requireEnvVariable('TEST_FUNCTIONAL_POSTGRES_URI'))
+      // The same server as js_pg, deliberately: the two runs are compared
+      // against each other, so a different server would make the comparison
+      // meaningless.
+      .with(AdapterProviders.JS_POSTGREJS, () => requireEnvVariable('TEST_FUNCTIONAL_POSTGRES_URI'))
       .with(AdapterProviders.JS_NEON, () => requireEnvVariable('TEST_FUNCTIONAL_POSTGRES_16_URI'))
       .with(AdapterProviders.JS_PLANETSCALE, () => requireEnvVariable('TEST_FUNCTIONAL_VITESS_8_URI'))
       .with(AdapterProviders.JS_LIBSQL, () => requireEnvVariable('TEST_FUNCTIONAL_LIBSQL_FILE_URI'))
